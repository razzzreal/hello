#include<stdio.h>//include standard i/o libraries in all the function 
    main()//start execution at mains
    {
        printf("hello chulbuli");//argument list in double quote there is a string constant
    }
